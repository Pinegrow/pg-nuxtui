import { pg_colors } from '~~/themes/pg-tailwindcss/tokens.cjs'

export default defineAppConfig({
  ui: {
    colors: {
      primary: 'primary',
    },
    // button: {
    //   slots: {
    //     base: 'font-bold'
    //   }
    // }
  },
})
